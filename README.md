Mi ultima entrega del proyecto de Angular,recordar:

En la carpeta database ejecutar los siguientes comandos para levantar el servidor json:

- npm install -g json-server

- json-server --watch db.json
